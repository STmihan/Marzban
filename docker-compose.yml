services:
  marzban:
    build: .
    container_name: marzban
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /var/lib/marzban:/var/lib/marzban
      - ./db:/code/db
      - ./config:/code/config
